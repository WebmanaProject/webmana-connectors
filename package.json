{
  "name": "webmana-connectors",
  "version": "0.1.0",
  "private": true,
  "description": "Apache-2.0 connector SDK for Webmana — contracts, connectors, and the scaffold CLI.",
  "license": "Apache-2.0",
  "packageManager": "pnpm@11.2.2",
  "engines": { "node": ">=22" },
  "scripts": {
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
